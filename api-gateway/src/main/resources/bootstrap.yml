spring:
  application:
    name: api-gateway
  cloud:
    config:
      discovery:
        enabled: true
        service-id: CONFIG
      profile: prod
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/